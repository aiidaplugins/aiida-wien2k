---
label: "wien2k-run123_lapw"
description: "wien2k run123_lapw"
input_plugin: "wien2k-run123_lapw"
on_computer: true
remote_abs_path: "/area51/WIEN2k_21/run123_lapw"
computer: "localhost"
prepend_text: '
export EDITOR="vim";
[[ -z "${SLURM_JOB_NAME}" ]] && export SCRATCH="/scratch/aiida/scratch-${RANDOM}" || export SCRATCH="/scratch/aiida/sctratch-${SLURM_JOB_NAME}";
mkdir -p ${SCRATCH};
mkdir -p case && cd case'
append_text: " "
